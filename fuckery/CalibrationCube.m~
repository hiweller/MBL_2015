

% read in deconvolution matrix

DeconMat = cell2mat(textscan(fopen('deconvolution.txt'), '%f'));

for i in 1:16
    index = DeconMat(((i-1)*16)+1):(i*16)
end

TransforMatrix = vec2mat(DeconMat(1:16), 4)

